var chai = require('chai') 
  , expect = chai.expect
  , JSONFile = require('../index')
  ;

describe('JSONFile', function () {

  it('should extend Backend');
  it('should initialize a JSON file');
  it('should save objects to the file');
  it('should find objects in the file');
  it('should update objects in the file');
  it('should destroy objects in the file');

});